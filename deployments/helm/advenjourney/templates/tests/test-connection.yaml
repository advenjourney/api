apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "advenjourney.fullname" . }}-test-connection"
  labels:
    {{- include "advenjourney.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "advenjourney.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
